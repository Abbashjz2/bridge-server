{
  "name": "billflow-device-services",
  "version": "1.0.0",
  "description": "BillFlow device services: monitor (ICMP ping), terminal (SSH WebSocket), and bridge (terminal + REST dashboard API)",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon -L index.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "node-fetch": "^2.7.0",
    "node-routeros": "^1.6.8",
    "ssh2": "^1.16.0",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
