services:
  noc-server:
    build: .
    container_name: noc-server
    hostname: bridge_server
    restart: unless-stopped
    ports:
      - "3066:3066"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    command: npm run dev